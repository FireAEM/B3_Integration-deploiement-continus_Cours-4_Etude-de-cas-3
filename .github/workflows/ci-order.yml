name: Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x, 24.x]

    steps:
      # Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Setup Node.js
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      # Cache node_modules for speed
      - name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # Install dependencies
      - name: Install dependencies
        run: npm install
        working-directory: services/order

      # Run lint (optional)
      - name: Run lint
        run: |
          if [ -f .eslintrc.json ] || [ -f .eslintrc.js ]; then
            npx eslint src tests
          else
            echo "No ESLint config found, skipping lint"
          fi
        working-directory: services/order

      # Run tests
      - name: Run tests
        run: npm test
        working-directory: services/order

      # Validate JSON file
      - name: Validate orders.json
        run: |
          if [ -f ./data/orders.json ]; then
            node -e "JSON.parse(require('fs').readFileSync('./data/orders.json')); console.log('orders.json is valid')"
          else
            echo "orders.json not found, skipping validation"
          fi
        working-directory: services/order
